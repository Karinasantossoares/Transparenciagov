<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:paddingHorizontal="4dp"
    android:layout_width="100dp"
    android:layout_height="wrap_content">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/imageViewCategory"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginBottom="16dp"
        android:padding="16dp"
        android:background="@drawable/shape_gradient_oval"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:src="@drawable/ic_microphones" />

    <TextView
        android:id="@+id/textCategory"
        android:layout_width="84dp"
        android:gravity="center"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:fontFamily="@font/montserrat"
        android:lines="1"
        android:maxLines="1"
        android:ellipsize="end"
        android:autoSizeTextType="uniform"
        android:autoSizeMinTextSize="10sp"
        android:textColor="@color/primary"
        app:layout_constraintStart_toStartOf="@id/imageViewCategory"
        app:layout_constraintEnd_toEndOf="@id/imageViewCategory"
        app:layout_constraintTop_toBottomOf="@id/imageViewCategory"
        android:text="@string/frentes" />
</androidx.constraintlayout.widget.ConstraintLayout>